# Скопируйте в .env и заполните (файл .env в репозиторий не попадает).
# Опционально: корень проекта, если config.yaml лежит не в cwd.
# MEET2TASK_ROOT=D:\path\to\meet2task

# https://console.groq.com/keys
GROQ_API_KEY=
# Токен от @BotFather
TELEGRAM_BOT_TOKEN=
